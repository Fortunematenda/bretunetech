/**
 * Shared React Hook Form + Zod helpers for Phase 2+ form migrations.
 */
export { useForm, Controller, FormProvider, useFormContext } from 'react-hook-form';
export { zodResolver } from '@hookform/resolvers/zod';
export { z } from 'zod';
