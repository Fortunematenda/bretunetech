/** Public API base used by the storefront and admin UI. */
export const DEFAULT_API_URL = 'https://api.bretunetech.com/api';

/** Canonical public site URL. */
export const DEFAULT_SITE_URL = 'https://bretunetech.com';
