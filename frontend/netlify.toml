[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "1"
  NODE_VERSION = "20"

# Production deploys via VPS (nginx + PM2). Do not add SPA /* → index.html
# rewrites here — that breaks Next.js App Router SSR and SEO.
